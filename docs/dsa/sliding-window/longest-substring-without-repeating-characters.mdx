---
sidebar_position: 2
title: Longest Substring Without Repeating Characters
---

import DryRunSlider from '@site/src/components/DryRunSlider';
import Difficulty from '@site/src/components/Difficulty';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Longest Substring Without Repeating Characters

<Difficulty level="Medium" />

> **LeetCode 3** – [Longest Substring Without Repeating Characters](https://leetcode.com/problems/longest-substring-without-repeating-characters/) | **Striver (TUF)** – [Longest Substring Without Repeating Characters Article](https://takeuforward.org/data-structure/length-of-longest-substring-without-any-repeating-character/)

## Problem Statement

Given a string `s`, find the length of the **longest substring** without repeating characters.

### Examples

| # | Input | Output |
|---|-------|--------|
| 1 | `s = "abcabcbb"` | `3` |
| 2 | `s = "bbbbb"` | `1` |
| 3 | `s = "pwwkew"` | `3` |

### Constraints

- `0 <= s.length <= 5 * 10⁴`
- `s` consists of English letters, digits, symbols and spaces.

---

## Solution

**Approach:** Use a sliding window with an array `h` of size 256 (all ASCII characters) that stores the **last seen index** of each character. When we encounter a character whose last index is within the current window (`>= l`), we shrink the window by moving `l` past that index.

- **Time Complexity:** $O(n)$ — single pass, each character visited once by `r`.
- **Space Complexity:** $O(1)$ — fixed-size array of 256 entries.

<Tabs groupId="lang">
<TabItem value="js" label="JavaScript">

```js
/**
 * @param {string} s
 * @return {number}
 */
var lengthOfLongestSubstring = function(s) {
    let h= new Array(256).fill(-1)
    let l=0,r=0;
    let n=s.length
    let max=0
    while(r<n)
    {
        let idx= s.charCodeAt(r)

        if(h[idx]>=l)
        {
            l=h[idx]+1

        }

        max=Math.max(max,r-l+1)
        h[idx]=r
        r++
    }
    return max
};
```

</TabItem>
<TabItem value="java" label="Java">

```java
import java.util.HashMap;

class Solution {
    public int lengthOfLongestSubstring(String s) {
        HashMap<Character, Integer> lastIndex = new HashMap<>();
        int l = 0, max = 0;
        for (int r = 0; r < s.length(); r++) {
            char c = s.charAt(r);
            if (lastIndex.containsKey(c) && lastIndex.get(c) >= l) {
                l = lastIndex.get(c) + 1;
            }
            max = Math.max(max, r - l + 1);
            lastIndex.put(c, r);
        }
        return max;
    }
}
```

</TabItem>
</Tabs>

---

## Dry Run

<DryRunSlider
  title='Longest Substring Without Repeating — s = "abcabcbb"'
  steps={[
    {
      caption: 'r=0: “a” not seen. Window "a". len=1, max=1.',
      state: ['l=0', 'r=0', 'len=1', 'max=1'],
      viz: {
        arrays: [{
          label: 's',
          values: ['a', 'b', 'c', 'a', 'b', 'c', 'b', 'b'],
          states: {0: 'active'},
          pointers: [{index: 0, label: 'l'}, {index: 0, label: 'r'}],
        }],
        map: {label: 'lastIndex', entries: [['a', '0']], highlightKey: 'a'},
        notes: ['window = "a"', 'len = 1', 'max = 1'],
      },
      details: (<span><code>a</code> is new. Record <code>lastIndex[a]=0</code>. Window length <code>r-l+1 = 1</code>.</span>),
    },
    {
      caption: 'r=1: “b” not seen. Window "ab". len=2, max=2.',
      state: ['l=0', 'r=1', 'len=2', 'max=2'],
      viz: {
        arrays: [{
          label: 's',
          values: ['a', 'b', 'c', 'a', 'b', 'c', 'b', 'b'],
          states: {0: 'window', 1: 'active'},
          pointers: [{index: 0, label: 'l'}, {index: 1, label: 'r'}],
        }],
        map: {label: 'lastIndex', entries: [['a', '0'], ['b', '1']], highlightKey: 'b'},
        notes: ['window = "ab"', 'len = 2', 'max = 2'],
      },
      details: (<span><code>b</code> is new, so the window grows to <code>"ab"</code>. Update <b>max = 2</b>.</span>),
    },
    {
      caption: 'r=2: “c” not seen. Window "abc". len=3, max=3.',
      state: ['l=0', 'r=2', 'len=3', 'max=3'],
      viz: {
        arrays: [{
          label: 's',
          values: ['a', 'b', 'c', 'a', 'b', 'c', 'b', 'b'],
          states: {0: 'match', 1: 'match', 2: 'match'},
          pointers: [{index: 0, label: 'l'}, {index: 2, label: 'r'}],
        }],
        map: {label: 'lastIndex', entries: [['a', '0'], ['b', '1'], ['c', '2']], highlightKey: 'c'},
        notes: ['window = "abc"', 'len = 3', 'max = 3  ★'],
      },
      details: (<span><code>c</code> is new — window becomes <code>"abc"</code>, our best so far (green). <b>max = 3</b>.</span>),
    },
    {
      caption: 'r=3: “a” last seen at 0 ≥ l=0 → repeat! Move l to 1. Window "bca".',
      state: ['l=1', 'r=3', 'len=3', 'max=3'],
      viz: {
        arrays: [{
          label: 's',
          values: ['a', 'b', 'c', 'a', 'b', 'c', 'b', 'b'],
          states: {0: 'muted', 1: 'window', 2: 'window', 3: 'active'},
          pointers: [{index: 1, label: 'l'}, {index: 3, label: 'r'}],
        }],
        map: {label: 'lastIndex', entries: [['a', '3'], ['b', '1'], ['c', '2']], highlightKey: 'a'},
        notes: ['a repeats (lastIndex 0 ≥ l)', 'l = 0+1 = 1', 'window = "bca", len = 3'],
      },
      details: (<span><code>lastIndex[a]=0 ≥ l</code>, so <code>a</code> is inside the window. Jump <code>l = lastIndex[a]+1 = 1</code>, dropping the old <code>a</code>. Update <code>lastIndex[a]=3</code>.</span>),
    },
    {
      caption: 'r=4: “b” last seen at 1 ≥ l=1 → repeat! Move l to 2. Window "cab".',
      state: ['l=2', 'r=4', 'len=3', 'max=3'],
      viz: {
        arrays: [{
          label: 's',
          values: ['a', 'b', 'c', 'a', 'b', 'c', 'b', 'b'],
          states: {1: 'muted', 2: 'window', 3: 'window', 4: 'active'},
          pointers: [{index: 2, label: 'l'}, {index: 4, label: 'r'}],
        }],
        map: {label: 'lastIndex', entries: [['a', '3'], ['b', '4'], ['c', '2']], highlightKey: 'b'},
        notes: ['b repeats (lastIndex 1 ≥ l)', 'l = 1+1 = 2', 'window = "cab", len = 3'],
      },
      details: (<span><code>lastIndex[b]=1 ≥ l</code>, so move <code>l = 2</code>. Window <code>"cab"</code> stays length 3.</span>),
    },
    {
      caption: 'r=5: “c” last seen at 2 ≥ l=2 → repeat! Move l to 3. Window "abc".',
      state: ['l=3', 'r=5', 'len=3', 'max=3'],
      viz: {
        arrays: [{
          label: 's',
          values: ['a', 'b', 'c', 'a', 'b', 'c', 'b', 'b'],
          states: {2: 'muted', 3: 'window', 4: 'window', 5: 'active'},
          pointers: [{index: 3, label: 'l'}, {index: 5, label: 'r'}],
        }],
        map: {label: 'lastIndex', entries: [['a', '3'], ['b', '4'], ['c', '5']], highlightKey: 'c'},
        notes: ['c repeats (lastIndex 2 ≥ l)', 'l = 2+1 = 3', 'window = "abc", len = 3'],
      },
      details: (<span><code>lastIndex[c]=2 ≥ l</code>, so <code>l = 3</code>. Still length 3, no improvement.</span>),
    },
    {
      caption: 'r=6: “b” last seen at 4 ≥ l=3 → repeat! Move l to 5. Window "cb".',
      state: ['l=5', 'r=6', 'len=2', 'max=3'],
      viz: {
        arrays: [{
          label: 's',
          values: ['a', 'b', 'c', 'a', 'b', 'c', 'b', 'b'],
          states: {3: 'muted', 4: 'muted', 5: 'window', 6: 'active'},
          pointers: [{index: 5, label: 'l'}, {index: 6, label: 'r'}],
        }],
        map: {label: 'lastIndex', entries: [['a', '3'], ['b', '6'], ['c', '5']], highlightKey: 'b'},
        notes: ['b repeats (lastIndex 4 ≥ l)', 'l = 4+1 = 5', 'window = "cb", len = 2'],
      },
      details: (<span><code>lastIndex[b]=4 ≥ l</code>, so <code>l = 5</code>. Window shrinks to <code>"cb"</code>.</span>),
    },
    {
      caption: 'r=7: “b” last seen at 6 ≥ l=5 → repeat! Move l to 7. Window "b". Done, max=3.',
      state: ['l=7', 'r=7', 'len=1', 'max=3'],
      viz: {
        arrays: [{
          label: 's',
          values: ['a', 'b', 'c', 'a', 'b', 'c', 'b', 'b'],
          states: {5: 'muted', 6: 'muted', 7: 'window'},
          pointers: [{index: 7, label: 'l'}, {index: 7, label: 'r'}],
        }],
        map: {label: 'lastIndex', entries: [['a', '3'], ['b', '7'], ['c', '5']], highlightKey: 'b'},
        notes: ['b repeats (lastIndex 6 ≥ l)', 'l = 6+1 = 7', 'answer = max = 3'],
      },
      details: (<span><code>lastIndex[b]=6 ≥ l</code>, so <code>l = 7</code>. The loop ends; the longest window seen was <code>"abc"</code> with length <b>3</b>.</span>),
    },
  ]}
/>

**Example 1:** `s = "abcabcbb"`

| Step | `r` | `s[r]` | `h[idx]` | `h[idx] >= l?` | `l` (after) | Window | `r-l+1` | `max` |
|------|-----|--------|----------|----------------|-------------|--------|----------|-------|
| 1 | 0 | `a` | -1 | No | 0 | `"a"` | 1 | 1 |
| 2 | 1 | `b` | -1 | No | 0 | `"ab"` | 2 | 2 |
| 3 | 2 | `c` | -1 | No | 0 | `"abc"` | 3 | **3** |
| 4 | 3 | `a` | 0 | Yes (`0 >= 0`) | 1 | `"bca"` | 3 | 3 |
| 5 | 4 | `b` | 1 | Yes (`1 >= 1`) | 2 | `"cab"` | 3 | 3 |
| 6 | 5 | `c` | 2 | Yes (`2 >= 2`) | 3 | `"abc"` | 3 | 3 |
| 7 | 6 | `b` | 4 | Yes (`4 >= 3`) | 5 | `"cb"` | 2 | 3 |
| 8 | 7 | `b` | 6 | Yes (`6 >= 5`) | 7 | `"b"` | 1 | 3 |

**Result:** `3` ✅ (substring `"abc"`)

---

## Test Cases

```js
console.log(lengthOfLongestSubstring("abcabcbb")); // 3
console.log(lengthOfLongestSubstring("bbbbb"));    // 1
console.log(lengthOfLongestSubstring("pwwkew"));   // 3
console.log(lengthOfLongestSubstring(""));          // 0
console.log(lengthOfLongestSubstring(" "));         // 1
console.log(lengthOfLongestSubstring("dvdf"));      // 3
console.log(lengthOfLongestSubstring("abba"));      // 2
```
