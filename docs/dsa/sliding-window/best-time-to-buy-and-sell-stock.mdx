---
sidebar_position: 1
title: Best Time to Buy and Sell Stock
---

import DryRunSlider from '@site/src/components/DryRunSlider';

# Best Time to Buy and Sell Stock

> **LeetCode 121** – [Best Time to Buy and Sell Stock](https://leetcode.com/problems/best-time-to-buy-and-sell-stock/) | **Striver (TUF)** – [Best Time to Buy and Sell Stock Article](https://takeuforward.org/data-structure/stock-buy-and-sell/)

## Problem Statement

You are given an array `prices` where `prices[i]` is the price of a given stock on the `iᵗʰ` day.

You want to maximize your profit by choosing a **single day** to buy and a **different day in the future** to sell.

Return the maximum profit you can achieve. If no profit is possible, return `0`.

### Examples

| # | Input | Output |
|---|-------|--------|
| 1 | `prices = [7,1,5,3,6,4]` | `5` |
| 2 | `prices = [7,6,4,3,1]` | `0` |

### Constraints

- `1 <= prices.length <= 10⁵`
- `0 <= prices[i] <= 10⁴`

---

## Solution

**Approach:** Use two pointers — `i` tracks the buy day (minimum price so far) and `j` scans forward for the sell day. If `prices[j] > prices[i]`, calculate profit. Otherwise, move the buy pointer to `j` since we found a cheaper price.

- **Time Complexity:** $O(n)$ — single pass through the array.
- **Space Complexity:** $O(1)$ — only a few variables.

```js
/**
 * @param {number[]} prices
 * @return {number}
 */
var maxProfit = function(prices) {
    let max=0
    let i=0;
    let j=1
    while(j<prices.length)
    {
        if(prices[j]>prices[i])
        {
            max=Math.max(max,(prices[j]-prices[i]))

        }
        else{
            i=j
        }
        j++
    }
    return max
};
```

---

## Dry Run

<DryRunSlider
  title="Best Time to Buy and Sell Stock — prices = [7,1,5,3,6,4]"
  steps={[
    {
      caption: 'Init: buy pointer i=0 (price 7), scan pointer j=1. max profit = 0.',
      state: ['i=0', 'j=1', 'minPrice=7', 'max=0'],
      viz: {
        arrays: [{
          label: 'prices',
          values: [7, 1, 5, 3, 6, 4],
          states: {0: 'active'},
          pointers: [{index: 0, label: 'i (buy)'}, {index: 1, label: 'j'}],
        }],
        notes: ['profit = —', 'max = 0'],
      },
      details: (<span>Start with the buy day at index <code>0</code> and the scan day <code>j=1</code>. We look for the largest gap where a later price beats the buy price.</span>),
    },
    {
      caption: 'j=1: prices[1]=1 < prices[0]=7 → cheaper price found. Move buy pointer: i=1.',
      state: ['i=1', 'j=1', 'minPrice=1', 'max=0'],
      viz: {
        arrays: [{
          label: 'prices',
          values: [7, 1, 5, 3, 6, 4],
          states: {1: 'active'},
          pointers: [{index: 1, label: 'i (buy)'}, {index: 1, label: 'j'}],
        }],
        notes: ['1 < 7 → set i = j = 1', 'max = 0'],
      },
      details: (<span>Since <code>prices[j] &lt; prices[i]</code>, buying earlier can never beat buying here. We reset the buy pointer to <code>i = 1</code>.</span>),
    },
    {
      caption: 'j=2: prices[2]=5 > prices[1]=1 → profit 5-1=4. max=4. Window [1..2].',
      state: ['i=1', 'j=2', 'minPrice=1', 'profit=4', 'max=4'],
      viz: {
        arrays: [{
          label: 'prices',
          values: [7, 1, 5, 3, 6, 4],
          states: {1: 'window', 2: 'active'},
          pointers: [{index: 1, label: 'i (buy)'}, {index: 2, label: 'j'}],
        }],
        notes: ['profit = 5 - 1 = 4', 'max = 4'],
      },
      details: (<span>Selling on day <code>2</code> yields <code>5 - 1 = 4</code>. Update <b>max = 4</b>. The yellow window spans the buy day to today.</span>),
    },
    {
      caption: 'j=3: prices[3]=3 > prices[1]=1 → profit 2. max stays 4.',
      state: ['i=1', 'j=3', 'minPrice=1', 'profit=2', 'max=4'],
      viz: {
        arrays: [{
          label: 'prices',
          values: [7, 1, 5, 3, 6, 4],
          states: {1: 'window', 2: 'window', 3: 'active'},
          pointers: [{index: 1, label: 'i (buy)'}, {index: 3, label: 'j'}],
        }],
        notes: ['profit = 3 - 1 = 2', 'max = 4'],
      },
      details: (<span>Profit <code>3 - 1 = 2</code> is smaller than the current <b>max = 4</b>, so max is unchanged.</span>),
    },
    {
      caption: 'j=4: prices[4]=6 > prices[1]=1 → profit 5. New best! max=5.',
      state: ['i=1', 'j=4', 'minPrice=1', 'profit=5', 'max=5'],
      viz: {
        arrays: [{
          label: 'prices',
          values: [7, 1, 5, 3, 6, 4],
          states: {1: 'match', 2: 'window', 3: 'window', 4: 'match'},
          pointers: [{index: 1, label: 'i (buy)'}, {index: 4, label: 'j'}],
        }],
        notes: ['profit = 6 - 1 = 5', 'max = 5  ★'],
      },
      details: (<span>Selling on day <code>4</code> gives <code>6 - 1 = 5</code> — the best so far. Update <b>max = 5</b>. Buy day and sell day are highlighted green.</span>),
    },
    {
      caption: 'j=5: prices[5]=4 > prices[1]=1 → profit 3. max stays 5.',
      state: ['i=1', 'j=5', 'minPrice=1', 'profit=3', 'max=5'],
      viz: {
        arrays: [{
          label: 'prices',
          values: [7, 1, 5, 3, 6, 4],
          states: {1: 'window', 2: 'window', 3: 'window', 4: 'window', 5: 'active'},
          pointers: [{index: 1, label: 'i (buy)'}, {index: 5, label: 'j'}],
        }],
        notes: ['profit = 4 - 1 = 3', 'max = 5'],
      },
      details: (<span>Profit <code>4 - 1 = 3</code> does not beat <b>max = 5</b>. j reaches the end next.</span>),
    },
    {
      caption: 'j=6: out of bounds → loop ends. Answer = max = 5.',
      state: ['i=1', 'j=6', 'max=5'],
      viz: {
        arrays: [{
          label: 'prices',
          values: [7, 1, 5, 3, 6, 4],
          states: {1: 'match', 4: 'match'},
          pointers: [{index: 1, label: 'buy'}, {index: 4, label: 'sell'}],
        }],
        notes: ['answer = 5', 'buy@day1 (1), sell@day4 (6)'],
      },
      details: (<span>The scan pointer passes the end of the array. The maximum profit is <b>5</b>: buy on day 1 at price 1, sell on day 4 at price 6.</span>),
    },
  ]}
/>

**Example 1:** `prices = [7, 1, 5, 3, 6, 4]`

| Step | `i` | `j` | `prices[i]` | `prices[j]` | Condition | Profit | `max` | Action |
|------|-----|-----|-------------|-------------|-----------|--------|-------|--------|
| 1 | 0 | 1 | 7 | 1 | `1 < 7` | — | 0 | `i = j` → `i = 1` |
| 2 | 1 | 2 | 1 | 5 | `5 > 1` | 4 | 4 | Keep `i`, `j++` |
| 3 | 1 | 3 | 1 | 3 | `3 > 1` | 2 | 4 | Keep `i`, `j++` |
| 4 | 1 | 4 | 1 | 6 | `6 > 1` | **5** | **5** | Keep `i`, `j++` |
| 5 | 1 | 5 | 1 | 4 | `4 > 1` | 3 | 5 | Keep `i`, `j++` |
| — | — | 6 | — | — | — | — | — | `j < length` false → exit |

**Result:** `5` ✅ (buy at day 1 for `1`, sell at day 4 for `6`)

---

## Test Cases

```js
console.log(maxProfit([7, 1, 5, 3, 6, 4])); // 5
console.log(maxProfit([7, 6, 4, 3, 1]));     // 0
console.log(maxProfit([2, 4, 1]));            // 2
console.log(maxProfit([1]));                  // 0
console.log(maxProfit([1, 2]));               // 1
console.log(maxProfit([3, 1, 4, 8, 7, 2, 5])); // 7  (buy at 1, sell at 8)
```
