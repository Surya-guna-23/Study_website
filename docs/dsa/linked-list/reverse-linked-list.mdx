---
sidebar_position: 1
title: Reverse Linked List
---

import DryRunSlider from '@site/src/components/DryRunSlider';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Reverse Linked List

> **LeetCode 206** – [Reverse Linked List](https://leetcode.com/problems/reverse-linked-list/) | **Striver (TUF)** – [Reverse Linked List Article](https://takeuforward.org/data-structure/reverse-a-linked-list/)

## Problem Statement

Given the `head` of a singly linked list, reverse the list, and return the reversed list.

### Examples

| # | Input | Output |
|---|-------|--------|
| 1 | `head = [1,2,3,4,5]` | `[5,4,3,2,1]` |
| 2 | `head = [1,2]` | `[2,1]` |
| 3 | `head = []` | `[]` |

### Constraints

- `0 <= Number of nodes <= 5000`
- `-5000 <= Node.val <= 5000`

---

## Solution

**Approach:** Recursive — recurse to the end of the list, then on the way back, reverse the pointers. The last node becomes the new head.

- **Time Complexity:** $O(n)$ — visit each node once.
- **Space Complexity:** $O(n)$ — recursion stack depth.

<Tabs groupId="lang">
<TabItem value="js" label="JavaScript">

```js
/**
 * Definition for singly-linked list.
 * function ListNode(val, next) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.next = (next===undefined ? null : next)
 * }
 */
/**
 * @param {ListNode} head
 * @return {ListNode}
 */
var reverseList = function(head) {
  function reverse(head)
  {
    if(head===null || head.next===null)
    {
        return head
    }
    const newhead = reverse(head.next)
    const front=head.next
    front.next=head
    head.next=null
    return newhead
  }
   return reverse(head)
};
```

</TabItem>
<TabItem value="java" label="Java">

```java
class Solution {
    public ListNode reverseList(ListNode head) {
        if (head == null || head.next == null) {
            return head;
        }
        ListNode newHead = reverseList(head.next);
        ListNode front = head.next;
        front.next = head;
        head.next = null;
        return newHead;
    }
}
```

</TabItem>
</Tabs>

---

## Dry Run

<DryRunSlider
  title="Reverse Linked List — 1 → 2 → 3 → 4 → 5"
  steps={[
    {
      caption: 'Recurse down to the end: reverse(1) → reverse(2) → … → reverse(5).',
      viz: { arrays: [{label:'list', values:[1,2,3,4,5], states:{0:'active'}, pointers:[{index:0,label:'head'}]}], notes:['recursion descending','base case will be node 5'] },
      state: ['head = 1','call stack building'],
      details: (<>We recurse first with <code>reverse(head.next)</code> until we hit the base case. No pointers are rewired on the way down.</>),
    },
    {
      caption: 'Base case reached at node 5 (head.next === null). Return 5 as newhead.',
      viz: { arrays: [{label:'list', values:[1,2,3,4,5], states:{0:'done',1:'done',2:'done',3:'done',4:'match'}, pointers:[{index:4,label:'newhead'}]}], notes:['newhead = 5','unwinding starts'] },
      state: ['newhead = 5'],
      details: (<>Node <code>5</code> becomes the permanent new head and is bubbled up through every return.</>),
    },
    {
      caption: 'reverse(4): front = 5, set 5.next = 4, 4.next = null.',
      viz: { arrays: [{label:'list', values:[1,2,3,4,5], states:{0:'active',1:'active',2:'active',3:'done',4:'match'}, pointers:[{index:3,label:'head'},{index:4,label:'front'}]}], notes:['reversed so far: 5 → 4','newhead = 5'] },
      state: ['head = 4','front = 5'],
      details: (<>We point <code>front.next</code> back to <code>head</code>, then cut <code>head.next</code>. Now <code>5 → 4</code>.</>),
    },
    {
      caption: 'reverse(3): front = 4, set 4.next = 3, 3.next = null.',
      viz: { arrays: [{label:'list', values:[1,2,3,4,5], states:{0:'active',1:'active',2:'done',3:'done',4:'match'}, pointers:[{index:2,label:'head'},{index:3,label:'front'}]}], notes:['reversed so far: 5 → 4 → 3','newhead = 5'] },
      state: ['head = 3','front = 4'],
      details: (<>Same rewiring one level up: <code>4.next = 3</code>, <code>3.next = null</code>.</>),
    },
    {
      caption: 'reverse(2): front = 3, set 3.next = 2, 2.next = null.',
      viz: { arrays: [{label:'list', values:[1,2,3,4,5], states:{0:'active',1:'done',2:'done',3:'done',4:'match'}, pointers:[{index:1,label:'head'},{index:2,label:'front'}]}], notes:['reversed so far: 5 → 4 → 3 → 2','newhead = 5'] },
      state: ['head = 2','front = 3'],
      details: (<><code>3.next = 2</code>, <code>2.next = null</code>. Almost done.</>),
    },
    {
      caption: 'reverse(1): front = 2, set 2.next = 1, 1.next = null. Fully reversed.',
      viz: { arrays: [{label:'list', values:[5,4,3,2,1], states:{0:'match',1:'done',2:'done',3:'done',4:'done'}, pointers:[{index:0,label:'newhead'}]}], notes:['reversed: 5 → 4 → 3 → 2 → 1','newhead = 5'] },
      state: ['head = 1','front = 2','newhead = 5'],
      details: (<>The last rewire gives <code>2.next = 1</code> and <code>1.next = null</code>. The returned <code>newhead</code> is <code>5</code>.</>),
    },
  ]}
/>

**Example:** `head = [1, 2, 3, 4, 5]`

**Recursion going down:**

```
reverse(1) → reverse(2) → reverse(3) → reverse(4) → reverse(5)
                                                      ↳ returns 5 (base case)
```

**Unwinding (reversing pointers):**

| Return from | `head` | `front` | Action | List state |
|-------------|--------|---------|--------|------------|
| `reverse(5)` | 5 | — | Base case, return `5` | `1→2→3→4→5` |
| `reverse(4)` | 4 | 5 | `5.next = 4`, `4.next = null` | `1→2→3→4←5` |
| `reverse(3)` | 3 | 4 | `4.next = 3`, `3.next = null` | `1→2→3←4←5` |
| `reverse(2)` | 2 | 3 | `3.next = 2`, `2.next = null` | `1→2←3←4←5` |
| `reverse(1)` | 1 | 2 | `2.next = 1`, `1.next = null` | `1←2←3←4←5` |

**New head:** `5` → **Result:** `[5, 4, 3, 2, 1]` ✅

---

## Test Cases

```js
// Helper to create linked list from array
function toList(arr) {
    let dummy = new ListNode(0);
    let curr = dummy;
    for (let v of arr) { curr.next = new ListNode(v); curr = curr.next; }
    return dummy.next;
}

console.log(reverseList(toList([1,2,3,4,5]))); // [5,4,3,2,1]
console.log(reverseList(toList([1,2])));         // [2,1]
console.log(reverseList(toList([])));            // null
console.log(reverseList(toList([1])));           // [1]
```
