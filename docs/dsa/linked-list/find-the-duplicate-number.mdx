---
sidebar_position: 7
title: Find the Duplicate Number
---

import DryRunSlider from '@site/src/components/DryRunSlider';
import Difficulty from '@site/src/components/Difficulty';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Find the Duplicate Number

<Difficulty level="Medium" />

> **LeetCode 287** – [Find the Duplicate Number](https://leetcode.com/problems/find-the-duplicate-number/) | **Striver (TUF)** – [Find the Duplicate Number Article](https://takeuforward.org/data-structure/find-the-duplicate-in-an-array-of-n1-integers/)

## Problem Statement

Given an array of integers `nums` containing `n + 1` integers where each integer is in the range `[1, n]` inclusive, there is **only one repeated number**. Return this repeated number.

### Examples

| # | Input | Output |
|---|-------|--------|
| 1 | `nums = [1,3,4,2,2]` | `2` |
| 2 | `nums = [3,1,3,4,2]` | `3` |
| 3 | `nums = [3,3,3,3,3]` | `3` |

### Constraints

- `1 <= n <= 10⁵`
- `nums.length == n + 1`
- `1 <= nums[i] <= n`

---

## Solution

**Approach:** Use a `Set` to track numbers seen so far. The first number that already exists in the set is the duplicate.

- **Time Complexity:** $O(n)$ — single pass.
- **Space Complexity:** $O(n)$ — the set.

<Tabs groupId="lang">
<TabItem value="js" label="JavaScript">

```js
/**
 * @param {number[]} nums
 * @return {number}
 */
var findDuplicate = function(nums) {
    let res= new Set()
    for(let i=0;i<nums.length;i++)
    {
        if(res.has(nums[i]))
        {
            return nums[i]
        }
        else{
            res.add(nums[i])
        }
    }
};
```

</TabItem>
<TabItem value="java" label="Java">

```java
import java.util.HashSet;
import java.util.Set;

class Solution {
    public int findDuplicate(int[] nums) {
        Set<Integer> seen = new HashSet<>();
        for (int i = 0; i < nums.length; i++) {
            if (seen.contains(nums[i])) {
                return nums[i];
            }
            seen.add(nums[i]);
        }
        return -1;
    }
}
```

</TabItem>
</Tabs>

---

## Dry Run

<DryRunSlider
  title="Find the Duplicate Number — nums=[1,3,4,2,2]"
  steps={[
    {
      caption: 'i = 0, nums[0] = 1. Not in set → add 1.',
      viz: { arrays: [{label:'nums', values:[1,3,4,2,2], states:{0:'active'}, pointers:[{index:0,label:'i'}]}], map:{label:'seen', entries:[['1','✓']], highlightKey:'1'}, notes:['added 1'] },
      state: ['i = 0','nums[i] = 1','seen = {1}'],
      details: (<>We track visited values in a <code>Set</code>. The first value that is already present is the duplicate.</>),
    },
    {
      caption: 'i = 1, nums[1] = 3. Not in set → add 3.',
      viz: { arrays: [{label:'nums', values:[1,3,4,2,2], states:{0:'done',1:'active'}, pointers:[{index:1,label:'i'}]}], map:{label:'seen', entries:[['1','✓'],['3','✓']], highlightKey:'3'}, notes:['added 3'] },
      state: ['i = 1','nums[i] = 3','seen = {1,3}'],
      details: (<><code>3</code> is new — store it and continue.</>),
    },
    {
      caption: 'i = 2, nums[2] = 4. Not in set → add 4.',
      viz: { arrays: [{label:'nums', values:[1,3,4,2,2], states:{0:'done',1:'done',2:'active'}, pointers:[{index:2,label:'i'}]}], map:{label:'seen', entries:[['1','✓'],['3','✓'],['4','✓']], highlightKey:'4'}, notes:['added 4'] },
      state: ['i = 2','nums[i] = 4','seen = {1,3,4}'],
      details: (<><code>4</code> is new — store it.</>),
    },
    {
      caption: 'i = 3, nums[3] = 2. Not in set → add 2.',
      viz: { arrays: [{label:'nums', values:[1,3,4,2,2], states:{0:'done',1:'done',2:'done',3:'active'}, pointers:[{index:3,label:'i'}]}], map:{label:'seen', entries:[['1','✓'],['3','✓'],['4','✓'],['2','✓']], highlightKey:'2'}, notes:['added 2'] },
      state: ['i = 3','nums[i] = 2','seen = {1,3,4,2}'],
      details: (<><code>2</code> is new — store it. One element left.</>),
    },
    {
      caption: 'i = 4, nums[4] = 2. Already in set → return 2!',
      viz: { arrays: [{label:'nums', values:[1,3,4,2,2], states:{0:'done',1:'done',2:'done',3:'match',4:'match'}, pointers:[{index:4,label:'i'}]}], map:{label:'seen', entries:[['1','✓'],['3','✓'],['4','✓'],['2','✓']], highlightKey:'2'}, notes:['2 seen before → duplicate','return 2'] },
      state: ['i = 4','nums[i] = 2','duplicate = 2'],
      details: (<><code>seen.has(2)</code> is true — we found the repeated value and return <code>2</code>.</>),
    },
  ]}
/>

**Example 1:** `nums = [1, 3, 4, 2, 2]`

| Step | `i` | `nums[i]` | `res.has(nums[i])?` | `res` (after) | Action |
|------|-----|-----------|---------------------|---------------|--------|
| 1 | 0 | 1 | No | `{1}` | Add `1` |
| 2 | 1 | 3 | No | `{1, 3}` | Add `3` |
| 3 | 2 | 4 | No | `{1, 3, 4}` | Add `4` |
| 4 | 3 | 2 | No | `{1, 3, 4, 2}` | Add `2` |
| 5 | 4 | 2 | **Yes** | — | **Return `2`** ✅ |

---

## Test Cases

```js
console.log(findDuplicate([1, 3, 4, 2, 2])); // 2
console.log(findDuplicate([3, 1, 3, 4, 2])); // 3
console.log(findDuplicate([3, 3, 3, 3, 3])); // 3
console.log(findDuplicate([1, 1]));            // 1
console.log(findDuplicate([2, 5, 9, 6, 9, 3, 8, 9, 7, 1])); // 9
```
